apply plugin: "war"

bootWar {
    mainClass = "de.farue.autocut.AutocutApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
